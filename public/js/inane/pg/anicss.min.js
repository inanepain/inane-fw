const defaults={autoremove:true,};export default class AniCSS{constructor(selector,options={}){this._options=I.margeOptions(options,defaults);this._selector=selector;this._nodes=undefined;if(typeof selector!=='undefined'){this._nodes=Array.from(document.querySelectorAll(selector));for(let index=0;index<this._nodes.length;index++){const element=this._nodes[index];element.addEventListener('animationend',this.animationEnd);}}}
animate(animation){for(let index=0;index<this._nodes.length;index++){const element=this._nodes[index];element.dataset.animation=animation;element.classList.add(animation);}}
animationEnd(event){console.log('animationEnd',event,this);}}