import Dumper from'/js/inane/dumper.js';Dumper.dump('MODULE','Backbone','Load');const config={idAttribute:'id',modelName:'Example',get collectionName(){return this.modelName+'s';},get url(){return'/api/'+this.collectionName.toLowerCase();},};class Example extends Backbone.Model{preinitialize(){this.name=config.modelName;this.idAttribute=config.idAttribute;this.urlRoot=config.url;this.defaults={id:null,};}
parse(response){return response.hasOwnProperty('payload')?response.payload:response;}}
class Examples extends Backbone.Collection{preinitialize(){this.name=config.collectionName;this.url=config.url;this.sortAttribute=Example.prototype.idAttribute;this.model=Example;this.options={};}
comparator(model){return model.get(this.sortAttribute);}
parse(response){let self=this;if(response.hasOwnProperty('options'))Object.keys(response.options).map(key=>self.options[key]=response.options[key]);return response.payload;}
fetch(options){if(options===undefined)options={data:this.options};else if(options.data===undefined)options.data=this.options;else options=Object.assign({data:this.options},options);return Backbone.Collection.prototype.fetch.call(this,options);}}
export{Example,Examples};